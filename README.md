# gh-agent-viz

An interactive terminal UI for visualizing GitHub Copilot coding agent sessions. Built as a `gh` CLI extension.

## Status

🚧 Under construction — see the scaffolding PR for the full project setup.